/**
 * DI-токен, собирающий все адаптеры источников музыки. Добавить
 * провайдера — значит зарегистрировать его класс под этим токеном в
 * `AudioModule`; `AudioService` и все вызывающие остаются нетронутыми.
 */
export const AUDIO_PROVIDERS = Symbol('AUDIO_PROVIDERS');
