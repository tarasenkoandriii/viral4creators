/**
 * Первая запись корпуса — грабля с входом через Telegram («Тонкая
 * красная линия» §6.6, этап 9).
 *
 * ## Почему она в коде, а не «оператор наберёт руками»
 *
 * Это приёмочный пример всего §6: на ней проверяется весь путь от
 * записи до подсказки на шаге записи обучалки. Набранная руками, она
 * была бы у владельца и не была бы у разработчика, а разошедшиеся
 * тексты потом невозможно сверить.
 *
 * ## Почему заводится ЧЕРНОВИКОМ
 *
 * §15 п.1 — открытый вопрос владельцу: причину я не воспроизводил.
 * Механизм «код уходит в приложение Telegram, а не в SMS, когда есть
 * активная сессия» — поведение самого Telegram, в нашем репозитории он
 * не описан нигде; это ровно тот тип знания, ради которого заводится
 * слой опыта, и ровно тот случай, когда объяснение, которое потом
 * цитирует модель тысяче людей, не должно опираться на реконструкцию.
 * Поэтому запись ждёт кнопки «Опубликовать» — то есть подтверждения
 * человеком, как того и требует §6.5.
 *
 * ## Что проверено по коду, а что нет
 *
 * Проверено: в НАШЕМ продукте входа по SMS нет вовсе (вход — Telegram
 * Login Widget, `initData`, dev-обход), то есть речь про вход на сайт
 * заказчика внутри обучалки; живая сессия закрывается после двух минут
 * бездействия и предупреждает за полминуты
 * (`live-login-relay/src/config.ts`), причём поднята она с одной
 * минуты именно потому, что ожидание кода проходит без единого события
 * мыши и клавиатуры. Не проверено: сам механизм доставки кода в
 * приложение — он не наш.
 */

export const FIRST_EXPERIENCE = {
  scenario: 'CLIENT_SITE',
  stepId: 'record',
  text: {
    locale: 'ru',
    symptom:
      'Нажали вход через Telegram на сайте заказчика, ждёте SMS — код не приходит.',
    cause:
      'При активной сессии Telegram код доставляется в само приложение, а не сообщением. ' +
      'Плюс вход через Telegram открывается отдельным окном, и в живой записи его надо дождаться.',
    advice:
      'Проверьте Telegram на телефоне и в десктопном клиенте — код там. ' +
      'Живая сессия ждёт две минуты бездействия и предупреждает за полминуты до закрытия; ' +
      'если не успели — нажмите {{clientSiteWizard.liveRestartButton}}.',
  },
} as const;
