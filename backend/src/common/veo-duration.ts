/**
 * Длительность ролика Veo (ТЗ §16, этап 15) — вынесена из
 * `generation.service.ts` на этапе 67, чтобы ей мог пользоваться и
 * `postprod.service.ts` (эвристика тайминга субтитров в режиме `veo`,
 * см. `common/voiceover-script.ts`'s `heuristicCueTimings`) без
 * циклического импорта: `generation.service.ts` уже импортирует
 * `PostProductionService`, а обратный импорт константы из него дал бы
 * цикл модулей.
 *
 * Значение не настраивается — Veo рендерит ровно столько (по секундам
 * же считается и счёт, §26), поэтому это одна константа на весь бэкенд,
 * а не параметр.
 */
export const VIDEO_DURATION_SECONDS = 8;
