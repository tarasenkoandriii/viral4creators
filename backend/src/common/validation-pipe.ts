/**
 * Настройки глобальной валидации — одни на приложение и на его тесты.
 *
 * Вынесены из `main.ts` затем, чтобы контрактные тесты маршрутов
 * проверяли ТУ ЖЕ валидацию, что работает на проде, а не свою копию.
 * Разница тут не теоретическая: `forbidNonWhitelisted` — единственное,
 * что не даёт клиенту прислать лишнее поле и однажды оказаться его
 * источником («дайджест считает сервер», ТЗ «Тонкая красная линия»
 * §5.5). Копия настроек в тесте зелёная и при выключенном флаге в
 * `main.ts` — то есть проверяет саму себя.
 */

import { ValidationPipeOptions } from '@nestjs/common';

export const VALIDATION_PIPE_OPTIONS: ValidationPipeOptions = {
  /** Поля вне DTO отбрасываются. */
  whitelist: true,
  /** И не молча: лишнее поле — отказ, а не тихое игнорирование. */
  forbidNonWhitelisted: true,
  /** Тело превращается в экземпляр DTO — нужно для вложенных структур. */
  transform: true,
};
