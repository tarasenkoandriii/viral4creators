-- Этап 95 (прямой запрос владельца продукта: «возьми из Solar Shop
-- импорт картинок для блога и адаптируй в лендинг блог», далее
-- уточнено — «используй так чтобы можно было использовать для снятия
-- обучающих роликов»): обложка блога перезаливается в собственный Blob
-- вместо хотлинка на YouTube CDN (`blog-cover-image.ts`).
-- `thumbnailUrl` остаётся единственным полем показа (лендинг/админка не
-- меняются); новое поле — только исходный URL-кандидат, для бэкофилла
-- (`BlogGenerationService.runCoverImageBackfill`).
-- Написана руками — сеть до binaries.prisma.sh недоступна в песочнице
-- разработки (doc/CI.md).
ALTER TABLE "blog_posts" ADD COLUMN "sourceImageUrl" TEXT;
