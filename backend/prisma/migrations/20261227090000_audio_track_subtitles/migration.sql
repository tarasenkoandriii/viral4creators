-- Субтитры альтернативной звуковой дорожки (этап 141,
-- docs-tz/TZ-Multilingual-YouTube.md) — последний невыполненный пункт
-- «Делаем» этапа 138.
--
-- `.srt` кладётся в строку дорожки, а не считается при чтении: дорожка
-- переживает TTL-очистку сессий (`sessionId` без внешнего ключа), а
-- сессия — источник и сценария, и длины ролика. Считать на лету
-- значило бы потерять субтитр ровно тогда, когда оператор до него
-- дойдёт. Та же причина, по которой рядом уже лежит `speech`.
ALTER TABLE "video_audio_tracks"
  ADD COLUMN "subtitlesSrt" TEXT;
