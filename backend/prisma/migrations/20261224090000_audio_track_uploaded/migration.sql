-- Отметка оператора «дорожка залита» (этап 139,
-- docs-tz/TZ-Multilingual-YouTube.md).
--
-- Именно ОТМЕТКА, а не проверка: API звуковых дорожек у YouTube нет
-- вовсе — файлы заливает человек в Studio, и убедиться машиной, что
-- дорожка на месте, нельзя ничем. Отметка оператора — честная замена, и
-- в интерфейсе она так и называется.
--
-- Кто отметил, хранится рядом: у остальных ручных действий оператора в
-- этом продукте (модерация заявок, снятие приглашений) след есть, и
-- ровно по той же причине — разбор задним числом не на чем вести, если
-- следов нет.
ALTER TABLE "video_audio_tracks"
  ADD COLUMN "uploadedAt" TIMESTAMP(3),
  ADD COLUMN "uploadedById" TEXT;
