-- Миграция 22 (этап 46, ТЗ §29): движение камеры в манифесте бренда.
--
-- Часть стиля СЕРИИ, а не разовая настройка генерации, — поэтому живёт
-- рядом с `voiceMode` и так же замораживается в снимке манифеста: смена
-- значения в бренде не должна задним числом менять уже отснятые ролики.
--
-- Значения: 'none' (как было) и 'push-in' (медленный непрерывный наезд).
-- Строкой, а не enum: список будет расширяться направлениями (отъезд,
-- проезд вбок), и каждое расширение enum в Postgres — отдельная
-- миграция с блокировкой типа.

ALTER TABLE "brand_manifests" ADD COLUMN "cameraMove" TEXT NOT NULL DEFAULT 'none';
