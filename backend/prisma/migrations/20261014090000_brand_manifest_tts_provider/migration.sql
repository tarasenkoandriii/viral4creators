-- Resemble AI как второй провайдер синтеза речи
-- (doc/TTS-PROVIDER-ALTERNATIVES-SPEC.md, §4.2/6.2). Новая колонка
-- помечает, каким провайдером выпущен ttsVoiceId — без неё смена
-- активного TTS_PROVIDER на стенде делает существующие ttsVoiceId
-- бессмысленными без единой ошибки компиляции или валидации. NULL для
-- всех существующих строк (провайдер на момент записи не восстановим
-- постфактум) — сервисный слой проставляет значение для новых записей
-- сам, клиенту не доверяем.

ALTER TABLE "brand_manifests" ADD COLUMN "ttsProvider" TEXT;
