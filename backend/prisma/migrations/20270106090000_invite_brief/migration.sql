-- Бриф тестировщика: что проверять (аудит этапа 161, §2.3 ТЗ).
--
-- Экран `#/testing` называется брифом и первым пунктом должен отвечать
-- на «что проверять» — а отвечать было нечем: текста этого нигде не
-- существовало.
--
-- На приглашении, а не в общей настройке: участок у каждого
-- тестировщика свой, и общий бриф на всех означал бы, что каждый читает
-- инструкцию для кого-то другого.
ALTER TABLE "tester_invites" ADD COLUMN "brief" TEXT;
